(()=>{const cfg=window.APP_CONFIG||{};const c=supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);const dlg=document.getElementById('linksDialog'),list=document.getElementById('editLinksList'),open=document.getElementById('editLinks'),close=document.getElementById('closeLinks'),all=document.getElementById('copyAllLinks'),msg=document.getElementById('allLinksStatus');if(!dlg||!open)return;let people=[];async function loadPeople(){const {data,error}=await c.rpc('get_admin_directory');if(error)throw error;people=data||[]}async function linkFor(id){const {data,error}=await c.rpc('get_admin_person_edit_token',{p_id:id});if(error||!data)throw error||new Error('No link');return `${location.origin}${location.pathname.replace(/directory\.html$/,'')}edit.html?token=${encodeURIComponent(data)}`}function draw(){list.innerHTML=people.map(p=>`<div class="edit-link-row"><div><strong>${p.name.replace(/[&<>"']/g,'')}</strong><small>Private profile edit link</small></div><button type="button" class="copy-edit-link" data-id="${p.id}">Copy link</button></div>`).join('')}open.onclick=async()=>{try{await loadPeople();draw();dlg.showModal()}catch(e){console.error(e);alert('Could not load edit links.')}};close.onclick=()=>dlg.close();list.onclick=async e=>{const b=e.target.closest('[data-id]');if(!b)return;const old=b.textContent;b.disabled=true;b.textContent='Copying…';try{await navigator.clipboard.writeText(await linkFor(b.dataset.id));b.textContent='Copied ✓';setTimeout(()=>{b.disabled=false;b.textContent=old},1800)}catch(err){console.error(err);b.disabled=false;b.textContent='Try again'}};all.onclick=async()=>{all.disabled=true;all.textContent='Preparing links…';msg.textContent='';try{const rows=[];for(const p of people)rows.push(`${p.name}: ${await linkFor(p.id)}`);await navigator.clipboard.writeText(rows.join('\n'));all.textContent='All links copied ✓';msg.textContent=`${rows.length} private links copied.`;setTimeout(()=>{all.disabled=false;all.textContent='Copy all links';msg.textContent=''},2500)}catch(e){console.error(e);all.disabled=false;all.textContent='Copy all links';msg.textContent='Could not copy all links.'}}})();