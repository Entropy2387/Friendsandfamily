(()=>{const cfg=window.APP_CONFIG||{};const client=supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);const grid=document.getElementById('peopleGrid'),status=document.getElementById('directoryStatus'),search=document.getElementById('peopleSearch'),count=document.getElementById('peopleCount'),dialog=document.getElementById('personDialog'),details=document.getElementById('personDetails');let people=[];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const val=v=>v?esc(v):'<span class="empty-value">Not added yet</span>';function age(d){if(!d)return'';const b=new Date(d+'T12:00:00'),n=new Date();let a=n.getFullYear()-b.getFullYear();if(n<new Date(n.getFullYear(),b.getMonth(),b.getDate()))a--;return a}function birthday(d){if(!d)return'Birthday not added';return new Date(d+'T12:00:00').toLocaleDateString('en-GB',{day:'numeric',month:'long'})}function render(list){count.textContent=`${list.length} ${list.length===1?'person':'people'}`;grid.innerHTML=list.map((p,i)=>`<button class="person-card" data-id="${p.id}"><div class="person-initial">${esc(p.name?.[0]||'♡')}</div><h2>${esc(p.name)}</h2><p>🎂 ${birthday(p.birthday)}${p.birthday?` · ${age(p.birthday)}`:''}</p><p>⌂ ${esc(p.household_name||'No household')}</p><span>View details →</span></button>`).join('');grid.hidden=false;status.hidden=true}function openPerson(p){details.innerHTML=`<p class="eyebrow">ADDRESS BOOK PROFILE</p><h2>${esc(p.name)} <span class="heart">♡</span></h2><div class="detail-grid"><div><b>Birthday</b>${val(p.birthday?new Date(p.birthday+'T12:00:00').toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'}):'')}</div><div><b>Household</b>${val(p.household_name)}</div><div class="wide"><b>Address</b>${val(p.address)}</div><div><b>Phone</b>${val(p.phone)}</div><div><b>Email</b>${val(p.email)}</div><div><b>Favourite flower</b>${val(p.favourite_flower)}</div><div><b>Favourite treat</b>${val(p.favourite_treat)}</div><div><b>Favourite drink</b>${val(p.favourite_drink)}</div><div class="wide"><b>Hobbies</b>${val(p.hobbies)}</div><div class="wide"><b>Likes</b>${val(p.likes)}</div><div class="wide"><b>Dislikes</b>${val(p.dislikes)}</div></div>`;dialog.showModal()}grid.addEventListener('click',e=>{const card=e.target.closest('.person-card');if(card)openPerson(people.find(p=>p.id===card.dataset.id))});search.addEventListener('input',()=>{const q=search.value.toLowerCase().trim();render(people.filter(p=>Object.values(p).some(v=>String(v??'').toLowerCase().includes(q))))});document.getElementById('closePerson').onclick=()=>dialog.close();document.getElementById('signOut').onclick=async()=>{await client.auth.signOut();location.replace('./')};async function init(){const {data:{session}}=await client.auth.getSession();if(!session){location.replace('./');return}const {data,error}=await client.rpc('get_admin_directory');if(error){console.error(error);status.textContent='Your address book could not be loaded.';return}people=data||[];render(people)}init()})();