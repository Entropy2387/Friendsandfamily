(()=>{const cfg=window.APP_CONFIG||{};const client=supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey);const form=document.getElementById('loginForm');const status=document.getElementById('loginStatus');client.auth.getSession().then(({data})=>{if(data.session)location.replace('directory.html')});form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Signing in…';const email=document.getElementById('loginEmail').value.trim();const password=document.getElementById('loginPassword').value;const {error}=await client.auth.signInWithPassword({email,password});if(error){status.textContent='Email or password not recognised.';return}location.replace('directory.html')})})();